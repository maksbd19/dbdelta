export function addNewConnection(config) {
  console.log('adding new connection from conig: ', config);
}

export function removeConnection(id) {
  console.log('removing connection: ' + id);
}

export function getConnectionDetail(id) {
  console.log('retrieving connection detail: ' + id);
}

export function updateConnection(id, config) {
  console.log('updating connection: ' + id, config);
}

export function getAllConnections(id, config) {
  console.log('retrieving all connections');
}
